import React from "react";

// ? Note props should not be changed with high frequency else memoized the props from the parent component..
const WarehouseDetails = React.memo(() => {
  return <div>WarehouseDetails</div>;
});

export default WarehouseDetails;
